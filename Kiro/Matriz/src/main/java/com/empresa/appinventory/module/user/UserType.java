package com.empresa.appinventory.module.user;

/**
 * Enumeration representing the type of user in the system.
 * Values must match the ENUM definition in the database schema.
 */
public enum UserType {
    Interno,
    Practicante,
    Contractor
}
